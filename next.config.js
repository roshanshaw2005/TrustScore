/** @type {import('next').NextConfig} */
const nextConfig = {
  // all your settings merged into ONE object here
  images: {
    domains: ["yourdomain.com"],
  },
  reactStrictMode: true,
  // ...etc
};

module.exports = nextConfig;